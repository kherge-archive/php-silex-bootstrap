{% extends "layout.html.twig" %}

{% form_theme form "form.html.twig" %}

{% block content %}
    <div class="container">
      <div class="row-fluid">
        <div class="span5 pull-both">
          <h1 class="page-header">{{ "hello"|trans({"{name}": name}) }}</h1>
          {{ form(form) }}
        </div>
      </div>
    </div>
{% endblock %}
